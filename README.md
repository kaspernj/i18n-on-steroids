# I18n on steroids

I18n for JavaScript using Node and the browser.

## Release

Do not edit the package version manually in feature branches or pull requests.

Bump the version and publish through the separate release flow using the repo's standard `release:patch` step.
